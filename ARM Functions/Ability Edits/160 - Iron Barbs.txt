Ability: Iron Barbs (ID: 160)
==================================================

Effect:
Damages attacking Pokémon for 1/8 their max HP on contact.

Dynamic ARM Mechanic:
- **Fractional Math (Healing):** The ARM code loads the Max HP from the Pokémon's stat struct. It then uses a logical shift right (`lsr`) instruction (`lsr #4` for 1/16th, `lsr #3` for 1/8th) to efficiently divide the Max HP by a power of 2.
- **Subroutine:** It calls the global healing subroutine (`bl`), passing the shifted value as the heal amount.
- **Contact Flag Check:** Before applying the effect, the code loads the move data struct of the incoming attack and performs a bitwise check (`tst` or `and`) against the 'Makes Contact' bitflag. If the flag is 0, it skips the effect.

Execution Hooks:
--------------------
Address: 0x0002AD74 (ARM)
Raw Assembly:
0x2AD74: A00050E3  cmp	r0, #0xa0
0x2AD78: 6CFFFF1A  bne	#0x2ab30
0x2AD7C: 0120A0E3  mov	r2, #1
0x2AD80: 1C108DE2  add	r1, sp, #0x1c

Logic Translation:
- `0x2AD74`: Compares the loaded ability ID against 0xa0. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x2AD78`: If it doesn't match, branches to `0x0x2ab30`.
- `0x2AD7C`: Loads literal `#1` into `r2`.
- `0x2AD80`: Adds `r1, sp, #0x1c` to calculate the modifier.
--------------------
Address: 0x000CA10C (ARM)
Raw Assembly:
0xCA10C: A00054E3  cmp	r4, #0xa0
0xCA110: 0100009A  bls	#0xca11c
0xCA114: A040A0E3  mov	r4, #0xa0
0xCA118: 020000EA  b	#0xca128

Logic Translation:
- `0xCA10C`: Compares the loaded ability ID against 0xa0. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0xCA110`: Calls the engine subroutine at `#0xca11c` to commit the math.
- `0xCA114`: Loads literal `#0xa0` into `r4`.
- `0xCA118`: Executes `b #0xca128`.
--------------------
Address: 0x000D5CEC (ARM)
Raw Assembly:
0xD5CEC: A00050E3  cmp	r0, #0xa0
0xD5CF0: 1570A003  moveq	r7, #0x15
0xD5CF4: 0070A013  movne	r7, #0
0xD5CF8: 0520A0E1  mov	r2, r5

Logic Translation:
- `0xD5CEC`: Compares the loaded ability ID against 0xa0. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0xD5CF0`: Loads literal `#0x15` into `r7`.
- `0xD5CF4`: Loads literal `#0` into `r7`.
- `0xD5CF8`: Loads literal `r5` into `r2`.
--------------------
