Ability: Flare Boost (ID: 138)
==================================================

Effect:
Increases Special Attack to 1.5× when burned.

Dynamic ARM Mechanic:
- **Multiplier Injection:** The engine loads the fixed-point hex value `0x1800` (which is 1.5x in 4096-based fixed-point math) into a register.
- **Calculation:** It then multiplies the base damage/stat by this register using `mul`.
- **Positive Stat Modifier:** The code loads a positive integer (e.g., `mov rX, #1` or `#2`) for the stat stage increase and calls the modifier subroutine to boost the target's stat array.
- **Status Affliction/Check:** The code loads the specific bitflag representing the status condition (e.g., `0x8` for Poison, `0x10` for Burn) into a register. It either bitwise-ANDs (`and`) it to check for the status, or bitwise-ORs (`orr`) it into the target's volatile status memory block to inflict it.

Execution Hooks:
--------------------
Address: 0x00077908 (ARM)
Raw Assembly:
0x77908: 8A0052E3  cmp	r2, #0x8a
0x7790C: D4019F05  ldreq	r0, [pc, #0x1d4]
0x77910: 4BFFFF0A  beq	#0x77644
0x77914: 8D0052E3  cmp	r2, #0x8d

Logic Translation:
- `0x77908`: Compares the loaded ability ID against 0x8a. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x7790C`: Loads memory data into `r0`.
- `0x77910`: If it matches, branches to `0x0x77644` to apply the effect.
- `0x77914`: Compares the loaded ability ID against 0x8d. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x000BC528 (THUMB)
Raw Assembly:
0xBC528: 8A2D      cmp	r5, #0x8a

Logic Translation:
- `0xBC528`: Compares the loaded ability ID against 0x8a. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x000BD9B0 (THUMB)
Raw Assembly:
0xBD9B0: 8A2F      cmp	r7, #0x8a
0xBD9B2: A0E3      b	#0xbe0f6
0xBD9B4: 0210      asrs	r2, r0, #0x20
0xBD9B6: A0E3      b	#0xbe0fa
0xBD9B8: 0400      movs	r4, r0

Logic Translation:
- `0xBD9B0`: Compares the loaded ability ID against 0x8a. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0xBD9B2`: Executes `b #0xbe0f6`.
- `0xBD9B4`: Executes `asrs r2, r0, #0x20`.
- `0xBD9B6`: Executes `b #0xbe0fa`.
- `0xBD9B8`: Loads literal `r0` into `r4`.
--------------------
