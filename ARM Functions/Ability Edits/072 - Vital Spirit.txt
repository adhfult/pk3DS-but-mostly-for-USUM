Ability: Vital Spirit (ID: 72)
==================================================

Effect:
Prevents sleep.

Dynamic ARM Mechanic:
- **Status Affliction/Check:** The code loads the specific bitflag representing the status condition (e.g., `0x8` for Poison, `0x10` for Burn) into a register. It either bitwise-ANDs (`and`) it to check for the status, or bitwise-ORs (`orr`) it into the target's volatile status memory block to inflict it.
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x0006AC08 (ARM)
Raw Assembly:
0x6AC08: 480052E3  cmp	r2, #0x48
0x6AC0C: C2FFFFBA  blt	#0x6ab1c
0x6AC10: 01DB8DE2  add	sp, sp, #0x400
0x6AC14: C3DF8DE2  add	sp, sp, #0x30c

Logic Translation:
- `0x6AC08`: Compares the loaded ability ID against 0x48. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x6AC0C`: Calls the engine subroutine at `#0x6ab1c` to commit the math.
- `0x6AC10`: Adds `sp, sp, #0x400` to calculate the modifier.
- `0x6AC14`: Adds `sp, sp, #0x30c` to calculate the modifier.
--------------------
Address: 0x000CD44C (ARM)
Raw Assembly:
0xCD44C: 480050E3  cmp	r0, #0x48
0xCD450: 0300001A  bne	#0xcd464
0xCD454: 7040BDE8  pop	{r4, r5, r6, lr}
0xCD458: 0F10A0E3  mov	r1, #0xf

Logic Translation:
- `0xCD44C`: Compares the loaded ability ID against 0x48. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0xCD450`: If it doesn't match, branches to `0x0xcd464`.
- `0xCD454`: Executes `pop {r4, r5, r6, lr}`.
- `0xCD458`: Loads literal `#0xf` into `r1`.
--------------------
Address: 0x000BCE30 (THUMB)
Raw Assembly:
0xBCE30: 482B      cmp	r3, #0x48

Logic Translation:
- `0xBCE30`: Compares the loaded ability ID against 0x48. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
