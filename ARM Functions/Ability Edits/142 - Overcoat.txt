Ability: Overcoat (ID: 142)
==================================================

Effect:
Protects against damage from weather.

Dynamic ARM Mechanic:
- **Weather Check:** The code reads the global battle state struct and compares the active Weather ID byte (`cmp rX, #WeatherID`). If the weather doesn't match the required state, it uses a `bne` (branch if not equal) to skip the effect.
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x000D19A8 (ARM)
Raw Assembly:
0xD19A8: 8E0050E3  cmp	r0, #0x8e
0xD19AC: 0C00000A  beq	#0xd19e4
0xD19B0: 0510A0E1  mov	r1, r5
0xD19B4: 0600A0E1  mov	r0, r6

Logic Translation:
- `0xD19A8`: Compares the loaded ability ID against 0x8e. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0xD19AC`: If it matches, branches to `0x0xd19e4` to apply the effect.
- `0xD19B0`: Loads literal `r5` into `r1`.
- `0xD19B4`: Loads literal `r6` into `r0`.
--------------------
Address: 0x000D3C5C (ARM)
Raw Assembly:
0xD3C5C: 8E0050E3  cmp	r0, #0x8e
0xD3C60: 1A00000A  beq	#0xd3cd0
0xD3C64: 0410A0E1  mov	r1, r4
0xD3C68: 0600A0E1  mov	r0, r6

Logic Translation:
- `0xD3C5C`: Compares the loaded ability ID against 0x8e. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0xD3C60`: If it matches, branches to `0x0xd3cd0` to apply the effect.
- `0xD3C64`: Loads literal `r4` into `r1`.
- `0xD3C68`: Loads literal `r6` into `r0`.
--------------------
Address: 0x00034A70 (THUMB)
Raw Assembly:
0x34A70: 8E2F      cmp	r7, #0x8e

Logic Translation:
- `0x34A70`: Compares the loaded ability ID against 0x8e. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
