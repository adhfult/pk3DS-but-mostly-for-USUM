Ability: Primordial Sea (ID: 189)
==================================================

Effect:
Creates heavy rain, which has all the properties of Rain Dance, cannot be replaced, and causes damaging Fire moves to fail.

Dynamic ARM Mechanic:
- **Weather Check:** The code reads the global battle state struct and compares the active Weather ID byte (`cmp rX, #WeatherID`). If the weather doesn't match the required state, it uses a `bne` (branch if not equal) to skip the effect.
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.
- **Weight Calculation:** The code reads the base weight integer from the species struct and uses it dynamically in the damage formula calculations (via division or multiplication).

Execution Hooks:
--------------------
Address: 0x00027FE8 (THUMB)
Raw Assembly:
0x27FE8: BD29      cmp	r1, #0xbd
0x27FEA: 00EB0B00  add.w	r0, r0, fp
0x27FEE: 50E1      b	#0x28292
0x27FF0: 00F020E3  blx	#0x428634
0x27FF4: 1400      movs	r4, r2

Logic Translation:
- `0x27FE8`: Compares the loaded ability ID against 0xbd. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x27FEA`: Adds `r0, r0, fp` to calculate the modifier.
- `0x27FEE`: Executes `b #0x28292`.
- `0x27FF0`: Calls the engine subroutine at `#0x428634` to commit the math.
- `0x27FF4`: Loads literal `r2` into `r4`.
--------------------
Address: 0x000361DC (THUMB)
Raw Assembly:
0x361DC: BD29      cmp	r1, #0xbd

Logic Translation:
- `0x361DC`: Compares the loaded ability ID against 0xbd. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x000BBC5C (THUMB)
Raw Assembly:
0xBBC5C: BD2F      cmp	r7, #0xbd

Logic Translation:
- `0xBBC5C`: Compares the loaded ability ID against 0xbd. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
