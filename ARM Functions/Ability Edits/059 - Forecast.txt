Ability: Forecast (ID: 59)
==================================================

Effect:
Changes Castform's type and form to match the weather.

Dynamic ARM Mechanic:
- **Weather Check:** The code reads the global battle state struct and compares the active Weather ID byte (`cmp rX, #WeatherID`). If the weather doesn't match the required state, it uses a `bne` (branch if not equal) to skip the effect.
- **Elemental Type Override:** The code loads the hex ID of the specific elemental type (e.g., `0x0` for Normal, `0x2` for Flying) and overwrites the Type 1 / Type 2 bytes in the active Pokémon's battle struct, temporarily changing its typing.
- **Form ID Override:** The engine writes a new integer to the Form ID byte in the Pokémon's data struct and triggers the form-change visual subroutine to update its model and stats.

Execution Hooks:
--------------------
Address: 0x0007D2B8 (ARM)
Raw Assembly:
0x7D2B8: 3B0051E3  cmp	r1, #0x3b
0x7D2BC: 0500001A  bne	#0x7d2d8
0x7D2C0: CC2290E5  ldr	r2, [r0, #0x2cc]
0x7D2C4: ED2F42E2  sub	r2, r2, #0x3b4

Logic Translation:
- `0x7D2B8`: Compares the loaded ability ID against 0x3b. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x7D2BC`: If it doesn't match, branches to `0x0x7d2d8`.
- `0x7D2C0`: Loads memory data into `r2`.
- `0x7D2C4`: Executes `sub r2, r2, #0x3b4`.
--------------------
Address: 0x00035114 (THUMB)
Raw Assembly:
0x35114: 3B2C      cmp	r4, #0x3b

Logic Translation:
- `0x35114`: Compares the loaded ability ID against 0x3b. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x000B5984 (THUMB)
Raw Assembly:
0xB5984: 3B2B      cmp	r3, #0x3b

Logic Translation:
- `0xB5984`: Compares the loaded ability ID against 0x3b. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
