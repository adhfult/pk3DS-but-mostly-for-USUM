Ability: Sand Veil (ID: 8)
==================================================

Effect:
Increases evasion to 1.25× during a sandstorm.  Protects against sandstorm damage.

Dynamic ARM Mechanic:
- **Weather Check:** The code reads the global battle state struct and compares the active Weather ID byte (`cmp rX, #WeatherID`). If the weather doesn't match the required state, it uses a `bne` (branch if not equal) to skip the effect.
- **Positive Stat Modifier:** The code loads a positive integer (e.g., `mov rX, #1` or `#2`) for the stat stage increase and calls the modifier subroutine to boost the target's stat array.
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x0001178C (ARM)
Raw Assembly:
0x1178C: 080050E3  cmp	r0, #8
0x11790: 230000AA  bge	#0x11824
0x11794: A01F80E0  add	r1, r0, r0, lsr #31
0x11798: 018000E2  and	r8, r0, #1

Logic Translation:
- `0x1178C`: Compares the loaded ability ID against 8. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x11790`: Executes `bge #0x11824`.
- `0x11794`: Adds `r1, r0, r0, lsr #31` to calculate the modifier.
- `0x11798`: Executes `and r8, r0, #1`.
--------------------
Address: 0x000182AC (ARM)
Raw Assembly:
0x182AC: 080051E3  cmp	r1, #8
0x182B0: 01F19F37  ldrlo	pc, [pc, r1, lsl #2]
0x182B4: AC0000EA  b	#0x1856c
0x182B8: 00000000  andeq	r0, r0, r0

Logic Translation:
- `0x182AC`: Compares the loaded ability ID against 8. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x182B0`: Loads memory data into `pc`.
- `0x182B4`: Executes `b #0x1856c`.
- `0x182B8`: Executes `andeq r0, r0, r0`.
--------------------
Address: 0x00019EE0 (ARM)
Raw Assembly:
0x19EE0: 080050E3  cmp	r0, #8
0x19EE4: 00F020E3  nop	
0x19EE8: 4500000A  beq	#0x1a004
0x19EEC: 48B901EB  bl	#0x88414

Logic Translation:
- `0x19EE0`: Compares the loaded ability ID against 8. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x19EE4`: Executes `nop `.
- `0x19EE8`: If it matches, branches to `0x0x1a004` to apply the effect.
- `0x19EEC`: Calls the engine subroutine at `#0x88414` to commit the math.
--------------------
