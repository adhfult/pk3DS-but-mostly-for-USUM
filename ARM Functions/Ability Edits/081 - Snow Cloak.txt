Ability: Snow Cloak (ID: 81)
==================================================

Effect:
Increases evasion to 1.25× during hail.  Protects against hail damage.

Dynamic ARM Mechanic:
- **Weather Check:** The code reads the global battle state struct and compares the active Weather ID byte (`cmp rX, #WeatherID`). If the weather doesn't match the required state, it uses a `bne` (branch if not equal) to skip the effect.
- **Positive Stat Modifier:** The code loads a positive integer (e.g., `mov rX, #1` or `#2`) for the stat stage increase and calls the modifier subroutine to boost the target's stat array.
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x0003883C (ARM)
Raw Assembly:
0x3883C: 510051E3  cmp	r1, #0x51
0x38840: 0B01000A  beq	#0x38c74
0x38844: 060000AA  bge	#0x38864
0x38848: 3C0051E3  cmp	r1, #0x3c

Logic Translation:
- `0x3883C`: Compares the loaded ability ID against 0x51. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x38840`: If it matches, branches to `0x0x38c74` to apply the effect.
- `0x38844`: Executes `bge #0x38864`.
- `0x38848`: Compares the loaded ability ID against 0x3c. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x000B7158 (ARM)
Raw Assembly:
0xB7158: 510050E3  cmp	r0, #0x51
0xB715C: 00F020E3  nop	
0xB7160: 0500000A  beq	#0xb717c
0xB7164: 010084E2  add	r0, r4, #1

Logic Translation:
- `0xB7158`: Compares the loaded ability ID against 0x51. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0xB715C`: Executes `nop `.
- `0xB7160`: If it matches, branches to `0x0xb717c` to apply the effect.
- `0xB7164`: Adds `r0, r4, #1` to calculate the modifier.
--------------------
Address: 0x000623C4 (THUMB)
Raw Assembly:
0x623C4: 512E      cmp	r6, #0x51
0x623C6: 81E2      b	#0x628cc
0x623C8: 0400      movs	r4, r0
0x623CA: 91E5      b	#0x61ef0
0x623CC: B000      lsls	r0, r6, #2

Logic Translation:
- `0x623C4`: Compares the loaded ability ID against 0x51. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x623C6`: Executes `b #0x628cc`.
- `0x623C8`: Loads literal `r0` into `r4`.
- `0x623CA`: Executes `b #0x61ef0`.
- `0x623CC`: Executes `lsls r0, r6, #2`.
--------------------
