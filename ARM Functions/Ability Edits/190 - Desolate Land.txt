Ability: Desolate Land (ID: 190)
==================================================

Effect:
Creates extremely harsh sunlight, which has all the properties of Sunny Day, cannot be replaced, and causes damaging Water moves to fail.

Dynamic ARM Mechanic:
- **Weather Check:** The code reads the global battle state struct and compares the active Weather ID byte (`cmp rX, #WeatherID`). If the weather doesn't match the required state, it uses a `bne` (branch if not equal) to skip the effect.
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.
- **Weight Calculation:** The code reads the base weight integer from the species struct and uses it dynamically in the damage formula calculations (via division or multiplication).

Execution Hooks:
--------------------
Address: 0x0002FE58 (THUMB)
Raw Assembly:
0x2FE58: BE2E      cmp	r6, #0xbe
0x2FE5A: 01EB0050  add.w	r0, r1, r0, lsl #20
0x2FE5E: A0E1      b	#0x301a2
0x2FE60: 0400      movs	r4, r0
0x2FE62: A0E1      b	#0x301a6

Logic Translation:
- `0x2FE58`: Compares the loaded ability ID against 0xbe. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x2FE5A`: Adds `r0, r1, r0, lsl #20` to calculate the modifier.
- `0x2FE5E`: Executes `b #0x301a2`.
- `0x2FE60`: Loads literal `r0` into `r4`.
- `0x2FE62`: Executes `b #0x301a6`.
--------------------
Address: 0x00034EE0 (THUMB)
Raw Assembly:
0x34EE0: BE2D      cmp	r5, #0xbe

Logic Translation:
- `0x34EE0`: Compares the loaded ability ID against 0xbe. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x000350C0 (THUMB)
Raw Assembly:
0x350C0: BE2D      cmp	r5, #0xbe

Logic Translation:
- `0x350C0`: Compares the loaded ability ID against 0xbe. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
