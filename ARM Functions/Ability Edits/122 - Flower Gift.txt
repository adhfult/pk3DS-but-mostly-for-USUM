Ability: Flower Gift (ID: 122)
==================================================

Effect:
Increases friendly Pokémon's Attack and Special Defense to 1.5× during strong sunlight.

Dynamic ARM Mechanic:
- **Multiplier Injection:** The engine loads the fixed-point hex value `0x1800` (which is 1.5x in 4096-based fixed-point math) into a register.
- **Calculation:** It then multiplies the base damage/stat by this register using `mul`.
- **Weather Check:** The code reads the global battle state struct and compares the active Weather ID byte (`cmp rX, #WeatherID`). If the weather doesn't match the required state, it uses a `bne` (branch if not equal) to skip the effect.
- **Positive Stat Modifier:** The code loads a positive integer (e.g., `mov rX, #1` or `#2`) for the stat stage increase and calls the modifier subroutine to boost the target's stat array.
- **Weight Calculation:** The code reads the base weight integer from the species struct and uses it dynamically in the damage formula calculations (via division or multiplication).

Execution Hooks:
--------------------
Address: 0x00031588 (THUMB)
Raw Assembly:
0x31588: 7A28      cmp	r0, #0x7a
0x3158A: 01EB0E00  add.w	r0, r1, lr
0x3158E: 50E3      b	#0x31c32
0x31590: 0C00      movs	r4, r1
0x31592: 000A      lsrs	r0, r0, #8

Logic Translation:
- `0x31588`: Compares the loaded ability ID against 0x7a. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x3158A`: Adds `r0, r1, lr` to calculate the modifier.
- `0x3158E`: Executes `b #0x31c32`.
- `0x31590`: Loads literal `r1` into `r4`.
- `0x31592`: Logical shift right by `8` (division by 2^N).
--------------------
Address: 0x00035818 (THUMB)
Raw Assembly:
0x35818: 7A2A      cmp	r2, #0x7a

Logic Translation:
- `0x35818`: Compares the loaded ability ID against 0x7a. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x00086C38 (THUMB)
Raw Assembly:
0x86C38: 7A2F      cmp	r7, #0x7a
0x86C3A: A0E3      b	#0x8737e
0x86C3C: 0000      movs	r0, r0
0x86C3E: 8DE5      b	#0x8675c
0x86C40: 0C00      movs	r4, r1

Logic Translation:
- `0x86C38`: Compares the loaded ability ID against 0x7a. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x86C3A`: Executes `b #0x8737e`.
- `0x86C3C`: Loads literal `r0` into `r0`.
- `0x86C3E`: Executes `b #0x8675c`.
- `0x86C40`: Loads literal `r1` into `r4`.
--------------------
