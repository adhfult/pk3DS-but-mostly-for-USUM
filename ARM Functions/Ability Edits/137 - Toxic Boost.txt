Ability: Toxic Boost (ID: 137)
==================================================

Effect:
Increases Attack to 1.5× when poisoned.

Dynamic ARM Mechanic:
- **Multiplier Injection:** The engine loads the fixed-point hex value `0x1800` (which is 1.5x in 4096-based fixed-point math) into a register.
- **Calculation:** It then multiplies the base damage/stat by this register using `mul`.
- **Positive Stat Modifier:** The code loads a positive integer (e.g., `mov rX, #1` or `#2`) for the stat stage increase and calls the modifier subroutine to boost the target's stat array.
- **Status Affliction/Check:** The code loads the specific bitflag representing the status condition (e.g., `0x8` for Poison, `0x10` for Burn) into a register. It either bitwise-ANDs (`and`) it to check for the status, or bitwise-ORs (`orr`) it into the target's volatile status memory block to inflict it.

Execution Hooks:
--------------------
Address: 0x000778FC (ARM)
Raw Assembly:
0x778FC: 890052E3  cmp	r2, #0x89
0x77900: DC019F05  ldreq	r0, [pc, #0x1dc]
0x77904: 4EFFFF0A  beq	#0x77644
0x77908: 8A0052E3  cmp	r2, #0x8a

Logic Translation:
- `0x778FC`: Compares the loaded ability ID against 0x89. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x77900`: Loads memory data into `r0`.
- `0x77904`: If it matches, branches to `0x0x77644` to apply the effect.
- `0x77908`: Compares the loaded ability ID against 0x8a. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
Address: 0x00063ED4 (THUMB)
Raw Assembly:
0x63ED4: 892B      cmp	r3, #0x89
0x63ED6: 001B      subs	r0, r0, r4
0x63ED8: 0410      asrs	r4, r0, #0x20
0x63EDA: 94E5      b	#0x63a06
0x63EDC: FF30      adds	r0, #0xff

Logic Translation:
- `0x63ED4`: Compares the loaded ability ID against 0x89. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x63ED6`: Executes `subs r0, r0, r4`.
- `0x63ED8`: Executes `asrs r4, r0, #0x20`.
- `0x63EDA`: Executes `b #0x63a06`.
- `0x63EDC`: Adds `r0, #0xff` to calculate the modifier.
--------------------
Address: 0x000BBD2C (THUMB)
Raw Assembly:
0xBBD2C: 892F      cmp	r7, #0x89

Logic Translation:
- `0xBBD2C`: Compares the loaded ability ID against 0x89. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
