Ability: Battle Armor (ID: 4)
==================================================

Effect:
Protects against critical hits.

Dynamic ARM Mechanic:
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x000100D8 (ARM)
Raw Assembly:
0x100D8: 040054E3  cmp	r4, #4
0x100DC: F2FFFF3A  blo	#0x100ac
0x100E0: 0440A0E3  mov	r4, #4
0x100E4: 834AC5E5  strb	r4, [r5, #0xa83]

Logic Translation:
- `0x100D8`: Compares the loaded ability ID against 4. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x100DC`: Calls the engine subroutine at `#0x100ac` to commit the math.
- `0x100E0`: Loads literal `#4` into `r4`.
- `0x100E4`: Stores `r4` into memory.
--------------------
Address: 0x00010138 (ARM)
Raw Assembly:
0x10138: 040053E3  cmp	r3, #4
0x1013C: 0100A003  moveq	r0, #1
0x10140: 1B00000A  beq	#0x101b4
0x10144: 881AD5E5  ldrb	r1, [r5, #0xa88]

Logic Translation:
- `0x10138`: Compares the loaded ability ID against 4. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x1013C`: Loads literal `#1` into `r0`.
- `0x10140`: If it matches, branches to `0x0x101b4` to apply the effect.
- `0x10144`: Loads memory data into `r1`.
--------------------
Address: 0x00010394 (ARM)
Raw Assembly:
0x10394: 040054E3  cmp	r4, #4
0x10398: EDFFFF3A  blo	#0x10354
0x1039C: 0180A0E3  mov	r8, #1
0x103A0: 0C0095E5  ldr	r0, [r5, #0xc]

Logic Translation:
- `0x10394`: Compares the loaded ability ID against 4. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x10398`: Calls the engine subroutine at `#0x10354` to commit the math.
- `0x1039C`: Loads literal `#1` into `r8`.
- `0x103A0`: Loads memory data into `r0`.
--------------------
