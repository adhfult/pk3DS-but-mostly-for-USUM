Ability: Damp (ID: 6)
==================================================

Effect:
Prevents Self-Destruct, Explosion, and Aftermath from working while the Pokémon is in battle.

Dynamic ARM Mechanic:
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x00010040 (ARM)
Raw Assembly:
0x10040: 060050E3  cmp	r0, #6
0x10044: 0100A023  movhs	r0, #1
0x10048: 1200002A  bhs	#0x10098
0x1004C: AC109FE5  ldr	r1, [pc, #0xac]

Logic Translation:
- `0x10040`: Compares the loaded ability ID against 6. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x10044`: Loads literal `#1` into `r0`.
- `0x10048`: Executes `bhs #0x10098`.
- `0x1004C`: Loads memory data into `r1`.
--------------------
Address: 0x00010158 (ARM)
Raw Assembly:
0x10158: 060050E3  cmp	r0, #6
0x1015C: 0500A013  movne	r0, #5
0x10160: 1300001A  bne	#0x101b4
0x10164: 4C1A95E5  ldr	r1, [r5, #0xa4c]

Logic Translation:
- `0x10158`: Compares the loaded ability ID against 6. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x1015C`: Loads literal `#5` into `r0`.
- `0x10160`: If it doesn't match, branches to `0x0x101b4`.
- `0x10164`: Loads memory data into `r1`.
--------------------
Address: 0x000110FC (ARM)
Raw Assembly:
0x110FC: 060050E3  cmp	r0, #6
0x11100: 0600001A  bne	#0x11120
0x11104: 0D30A0E1  mov	r3, sp
0x11108: 04208DE2  add	r2, sp, #4

Logic Translation:
- `0x110FC`: Compares the loaded ability ID against 6. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x11100`: If it doesn't match, branches to `0x0x11120`.
- `0x11104`: Loads literal `sp` into `r3`.
- `0x11108`: Adds `r2, sp, #4` to calculate the modifier.
--------------------
