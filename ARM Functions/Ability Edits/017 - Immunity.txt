Ability: Immunity (ID: 17)
==================================================

Effect:
Prevents poison.

Dynamic ARM Mechanic:
- **Status Affliction/Check:** The code loads the specific bitflag representing the status condition (e.g., `0x8` for Poison, `0x10` for Burn) into a register. It either bitwise-ANDs (`and`) it to check for the status, or bitwise-ORs (`orr`) it into the target's volatile status memory block to inflict it.
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x00044360 (ARM)
Raw Assembly:
0x44360: 110050E3  cmp	r0, #0x11
0x44364: 4D00000A  beq	#0x444a0
0x44368: 0900A0E1  mov	r0, sb
0x4436C: 5AB900EB  bl	#0x728dc

Logic Translation:
- `0x44360`: Compares the loaded ability ID against 0x11. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x44364`: If it matches, branches to `0x0x444a0` to apply the effect.
- `0x44368`: Loads literal `sb` into `r0`.
- `0x4436C`: Calls the engine subroutine at `#0x728dc` to commit the math.
--------------------
Address: 0x00049CE8 (ARM)
Raw Assembly:
0x49CE8: 110050E3  cmp	r0, #0x11
0x49CEC: 0160A003  moveq	r6, #1
0x49CF0: 0400000A  beq	#0x49d08
0x49CF4: 0030A0E3  mov	r3, #0

Logic Translation:
- `0x49CE8`: Compares the loaded ability ID against 0x11. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x49CEC`: Loads literal `#1` into `r6`.
- `0x49CF0`: If it matches, branches to `0x0x49d08` to apply the effect.
- `0x49CF4`: Loads literal `#0` into `r3`.
--------------------
Address: 0x00054260 (ARM)
Raw Assembly:
0x54260: 110055E3  cmp	r5, #0x11
0x54264: 0800000A  beq	#0x5428c
0x54268: 100055E3  cmp	r5, #0x10
0x5426C: 1200000A  beq	#0x542bc

Logic Translation:
- `0x54260`: Compares the loaded ability ID against 0x11. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x54264`: If it matches, branches to `0x0x5428c` to apply the effect.
- `0x54268`: Compares the loaded ability ID against 0x10. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x5426C`: If it matches, branches to `0x0x542bc` to apply the effect.
--------------------
