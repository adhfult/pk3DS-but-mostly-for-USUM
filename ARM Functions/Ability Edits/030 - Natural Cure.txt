Ability: Natural Cure (ID: 30)
==================================================

Effect:
Cures any major status ailment upon switching out.

Dynamic ARM Mechanic:
- **Status Affliction/Check:** The code loads the specific bitflag representing the status condition (e.g., `0x8` for Poison, `0x10` for Burn) into a register. It either bitwise-ANDs (`and`) it to check for the status, or bitwise-ORs (`orr`) it into the target's volatile status memory block to inflict it.

Execution Hooks:
--------------------
Address: 0x000122E8 (ARM)
Raw Assembly:
0x122E8: 1E0050E3  cmp	r0, #0x1e
0x122EC: 00F020E3  nop	
0x122F0: 0400000A  beq	#0x12308
0x122F4: 000096E5  ldr	r0, [r6]

Logic Translation:
- `0x122E8`: Compares the loaded ability ID against 0x1e. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x122EC`: Executes `nop `.
- `0x122F0`: If it matches, branches to `0x0x12308` to apply the effect.
- `0x122F4`: Loads memory data into `r0`.
--------------------
Address: 0x0001CA94 (ARM)
Raw Assembly:
0x1CA94: 1E0051E3  cmp	r1, #0x1e
0x1CA98: 1E00A083  movhi	r0, #0x1e
0x1CA9C: 411EC4E5  strb	r1, [r4, #0xe41]
0x1CAA0: 410EC485  strbhi	r0, [r4, #0xe41]

Logic Translation:
- `0x1CA94`: Compares the loaded ability ID against 0x1e. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x1CA98`: Loads literal `#0x1e` into `r0`.
- `0x1CA9C`: Stores `r1` into memory.
- `0x1CAA0`: Stores `r0` into memory.
--------------------
Address: 0x0002AAA8 (ARM)
Raw Assembly:
0x2AAA8: 1E0050E3  cmp	r0, #0x1e
0x2AAAC: 1F0000BA  blt	#0x2ab30
0x2AAB0: 040094E5  ldr	r0, [r4, #4]
0x2AAB4: 049096E5  ldr	sb, [r6, #4]

Logic Translation:
- `0x2AAA8`: Compares the loaded ability ID against 0x1e. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x2AAAC`: Calls the engine subroutine at `#0x2ab30` to commit the math.
- `0x2AAB0`: Loads memory data into `r0`.
- `0x2AAB4`: Loads memory data into `sb`.
--------------------
