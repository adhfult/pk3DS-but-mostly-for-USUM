Ability: Own Tempo (ID: 20)
==================================================

Effect:
Prevents confusion.

Dynamic ARM Mechanic:
- **Immunity Bypass:** The code sets a global immunity flag in memory (`str rX, [rY]`) OR it executes a branch (`b` or `beq`) that literally jumps completely over the damage/status calculation subroutine, thereby preventing the effect from occurring.

Execution Hooks:
--------------------
Address: 0x000179FC (ARM)
Raw Assembly:
0x179FC: 140056E3  cmp	r6, #0x14
0x17A00: 450ED405  ldrbeq	r0, [r4, #0xe45]
0x17A04: 80008003  orreq	r0, r0, #0x80
0x17A08: 3D00000A  beq	#0x17b04

Logic Translation:
- `0x179FC`: Compares the loaded ability ID against 0x14. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x17A00`: Loads memory data into `r0`.
- `0x17A04`: Executes `orreq r0, r0, #0x80`.
- `0x17A08`: If it matches, branches to `0x0x17b04` to apply the effect.
--------------------
Address: 0x0002AA5C (ARM)
Raw Assembly:
0x2AA5C: 140050E3  cmp	r0, #0x14
0x2AA60: 0500A001  moveq	r0, r5
0x2AA64: 2302000A  beq	#0x2b2f8
0x2AA68: 320000CA  bgt	#0x2ab38

Logic Translation:
- `0x2AA5C`: Compares the loaded ability ID against 0x14. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x2AA60`: Loads literal `r5` into `r0`.
- `0x2AA64`: If it matches, branches to `0x0x2b2f8` to apply the effect.
- `0x2AA68`: Executes `bgt #0x2ab38`.
--------------------
Address: 0x0002BDC8 (ARM)
Raw Assembly:
0x2BDC8: 140050E3  cmp	r0, #0x14
0x2BDCC: 0500A093  movls	r0, #5
0x2BDD0: 0200009A  bls	#0x2bde0
0x2BDD4: 1E0050E3  cmp	r0, #0x1e

Logic Translation:
- `0x2BDC8`: Compares the loaded ability ID against 0x14. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
- `0x2BDCC`: Loads literal `#5` into `r0`.
- `0x2BDD0`: Calls the engine subroutine at `#0x2bde0` to commit the math.
- `0x2BDD4`: Compares the loaded ability ID against 0x1e. (Multiple cmp instructions in a row mean the engine is checking if the Pokémon has one of several abilities that share the exact same mechanic, like Mold Breaker/Teravolt/Turboblaze).
--------------------
